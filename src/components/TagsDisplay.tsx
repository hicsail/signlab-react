import { Box, Typography } from '@mui/material';

const TagsDisplay: React.FC = () => {
  return (
    <Box sx={{ width: 2400, height: '20rem' }}>
      <Typography variant="h4">Displaying Tags Component</Typography>
    </Box>
  );
};

export { TagsDisplay };
